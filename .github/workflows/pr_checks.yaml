name: phobos::checks

on:
  pull_request:
    branches:
      - dev
      - dev/*
      - main
      - main/*

jobs:
  run-build:
    name: phobos::checks::build
    runs-on: ubuntu-latest

    steps:
      - name: phobos::checks::build::Check out Git repository
        uses: actions/checkout@v3

      - name: phobos::checks::build::Set up Node.js
        uses: actions/setup-node@v1
        with:
          node-version: 20

      - name: phobos::checks::build::Install Node.js dependencies
        run: npm ci

      - name: phobos::checks::build::Build
        run: npm run build

  run-lint:
    name: phobos::checks::lint
    runs-on: ubuntu-latest

    steps:
      - name: phobos::checks::lint::Check out Git repository
        uses: actions/checkout@v3

      - name: phobos::checks::lint::Set up Node.js
        uses: actions/setup-node@v1
        with:
          node-version: 20

      # ESLint and Prettier must be in `package.json`
      - name: phobos::checks::lint::Install Node.js dependencies
        run: npm ci

      - name: phobos::checks::lint::Run linters
        run: npm run format
